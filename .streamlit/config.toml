[server]
# headless = true is required for cloud/container deployments
headless = true
enableCORS = false
enableXsrfProtection = false
# Do NOT set port or address — let the platform use its default (8501)

[browser]
gatherUsageStats = false
